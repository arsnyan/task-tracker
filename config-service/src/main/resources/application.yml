server:
  port: 8888

spring:
  profiles:
    active: git
  cloud:
    refresh:
      enabled: false
    config:
      server:
        git:
          uri: https://github.com/arsnyan/task-tracker
          search-paths: service-configs